specVersion: "v1beta"
serviceId: "debug"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "debug-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
